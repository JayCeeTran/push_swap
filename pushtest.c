//#include "push_swap.h"
#include <stdio.h>

/*stack  *ft_lstne_ps(int content)
{
        stack  *newnode;

        newnode = malloc(sizeof(stack));
        if (!newnode)
                return (NULL);
        newnode->val = content;
        newnode->next = NULL;
        newnode->prev = NULL;
        return (newnode);
}*/

int main(int argc, char **argv)
{
    /*stack *node;
    stack *stack_a;*/
    int i;

    i = 0;
    if (argc < 2)
        return (i);
    /*stack_a = NULL;
    if(stack_a == NULL)
        printf("YEE");
    node = ft_lstne_ps(12);*/
    printf("%d \n", i);
    //printsome();
    argv++;
    return(0);

}
