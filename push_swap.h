#ifndef PUSH_SWAP_H
# define PUSH_SWAP_H

#include "libft/libft.h"
//#include "libft_printf.h"
#include <stdlib.h>
#include <unistd.h>

typedef struct{

	int val;
	struct stack *next;
	struct stack *prev;
} stack;


#endif
